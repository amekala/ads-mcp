interface:
  display_name: "Adspirer Performance Marketing Agent"
  short_description: "Brand-aware paid media management across Google, Meta, LinkedIn & TikTok"
  icon_small: "./assets/adspirer-small.svg"
  icon_large: "./assets/adspirer.png"
  default_prompt: "Check connected ad platforms and summarize recent campaign performance for this brand. If this is a new workspace, set it up first."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "adspirer"
      description: "Adspirer MCP server for cross-platform ad management (100+ tools)"
      transport: "streamable_http"
      url: "https://mcp.adspirer.com/mcp"
