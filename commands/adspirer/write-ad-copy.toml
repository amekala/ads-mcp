prompt = """
Write ad copy for {{args}}.

1. Call `get_connections_status` to verify platform access
2. Call `get_campaign_structure` to see current ads and keywords
3. Call `analyze_search_terms` to see what users actually search
4. Call `suggest_ad_content` for AI-generated suggestions based on real performance data
5. Generate 5+ headline and description options
6. Present options in a table for review
"""
