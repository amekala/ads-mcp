---
description: Write brand-voice-compliant ad copy for a specific platform
argument-hint: <platform and campaign, e.g. "Google Search brand defense">
---
Write ad copy for $ARGUMENTS.

1. Read CLAUDE.md for brand voice rules and audience info
2. Call `mcp__adspirer__get_campaign_structure` to see current ads and keywords
3. Call `mcp__adspirer__analyze_search_terms` to see what users actually search
4. Call `mcp__adspirer__suggest_ad_content` for AI-generated suggestions based on real data
5. Generate 5+ headline/description options that follow brand guidelines and are informed by real performance data
6. Present options for user approval
